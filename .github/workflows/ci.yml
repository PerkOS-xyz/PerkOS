name: CI

on:
  push:
    branches: [main, development, qa]
  pull_request:
    branches: [main, development, qa]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test + typecheck + build
    runs-on: ubuntu-latest
    timeout-minutes: 12
    env:
      # Public Firebase config is safe in CI logs.
      NEXT_PUBLIC_FIREBASE_API_KEY: ci-stub
      NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ci.example.com
      NEXT_PUBLIC_FIREBASE_PROJECT_ID: perkos-ci
      NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: perkos-ci.appspot.com
      NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: "1"
      NEXT_PUBLIC_FIREBASE_APP_ID: "1:1:web:ci"
      NEXT_PUBLIC_PERKOS_CHAT_URL: wss://chat.perkos.xyz/chat
      PERKOS_WHITELIST: ""
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Typecheck
        run: npm run typecheck
      - name: Tests
        run: npm test
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Artizen Firestore rules (local demo only)
        run: npx --yes --package=firebase-tools@13.35.1 firebase emulators:exec --only firestore --project demo-artizen-workspace --config firebase.rules-test.json 'npx vitest run tests/artizenFirestoreRules.test.ts'
      - name: Build (production)
        run: npm run build
