/**
 * Re-export of display helpers (address ellipsis, relative time).
 * Implementations live in `@perkos/shared-client`.
 */
export { formatAddress, formatRelativeShort } from "@perkos/shared-client";
